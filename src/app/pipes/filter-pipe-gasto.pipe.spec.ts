import { FilterPipeGastoPipe } from './filter-pipe-gasto.pipe';

describe('FilterPipeGastoPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPipeGastoPipe();
    expect(pipe).toBeTruthy();
  });
});
