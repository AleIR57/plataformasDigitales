import { FilterPipeClientePipe } from './filter-pipe-cliente.pipe';

describe('FilterPipeClientePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPipeClientePipe();
    expect(pipe).toBeTruthy();
  });
});
